apiVersion: apps/v1
kind: Deployment
metadata:
  name: workspacedataentry-api-depl
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workspacedataentry-api
  template:
    metadata:
      labels:
        app: workspacedataentry-api
    spec:
      containers:
        - name: workspacedataentry-api
          image: domih/mcce-smart-office-workspacedataentry-api:latest
          imagePullPolicy: Always
          env:
            - name: SMARTOFFICE_BASEADDRESS
              value: http://workspacedataentry-api-clusterip-srv:8080
          envFrom:
            - configMapRef:
                name: env-configmap
          resources:
            requests:
              memory: "128Mi"
              cpu: "125m"
            limits:
              memory: "512Mi"
              cpu: "250m"
          volumeMounts:
            - name: workspacedataentry-api-config
              mountPath: /app/appsettings.json
              subPath: appsettings.json
      volumes:
        - name: workspacedataentry-api-config
          configMap:
            name: workspacedataentry-api-config
---
apiVersion: v1
kind: Service
metadata:
  name: workspacedataentry-api-clusterip-srv
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: workspacedataentry-api
  ports:
    - name: workspacedataentry-api
      protocol: TCP
      port: 80
      targetPort: 8080
